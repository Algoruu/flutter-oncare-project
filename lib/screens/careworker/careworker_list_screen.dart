import 'package:flutter/material.dart';

class CareworkerListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Careworker List Screen')),
    );
  }
}
