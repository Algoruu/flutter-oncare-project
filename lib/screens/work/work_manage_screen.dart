import 'package:flutter/material.dart';

class WorkManageScreen extends StatelessWidget {
  const WorkManageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Work Manage Screen')),
    );
  }
}
