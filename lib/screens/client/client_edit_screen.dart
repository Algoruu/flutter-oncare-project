import 'package:flutter/material.dart';

class ClientEditScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Client Edit Screen')),
    );
  }
}
