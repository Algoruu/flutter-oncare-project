import 'package:flutter/material.dart';

class JournalListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Journal List Screen')),
    );
  }
}
